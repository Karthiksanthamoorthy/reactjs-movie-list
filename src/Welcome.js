function Welcome({ name }) {
  return (
    <section>

      <h1 className="user-foo">Hello,  <span className="user-first-name">{name}😊😊 -{10 * 20} followers{" "}</span></h1>
    </section>
  );
}
